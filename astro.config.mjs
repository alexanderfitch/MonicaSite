// @ts-check
import { defineConfig } from 'astro/config';

// Static author website for Monica Messer.
// Outputs a fully static site to ./dist (no server needed to host).
export default defineConfig({
  // site: 'https://monicamesser.com', // set this to your domain before deploying
});
